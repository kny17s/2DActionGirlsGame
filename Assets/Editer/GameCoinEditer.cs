using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

/*[CustomEditor(typeof(GameCoinData))]
public class GameCoinEditer : Editor
{
    public override void OnInspectorGUI()
    {
        //元のInspector部分を表示
        base.OnInspectorGUI();

        GameCoinData gameCoinData = target as GameCoinData;

        if (GUILayout.Button("2000コインチャージ"))
        {
            gameCoinData.AddCoin(2000);
        }

        if (GUILayout.Button("1500ガチャコインチャージ"))
        {
            gameCoinData.AddGachaCoin(1500);
        }
    }
}*/
